server:
  port: 8081
dubbo:
  application:
    name: web
  registry:
    address: zookeeper://127.0.0.1:2181
  protocol:
    name: dubbo
    port: 20882

spring:
  datasource:
    driver: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gou?serverTimezone=UTC
    username: root
    password: 1

logging:
  level:
    com.llm.demo.mapper.*: debug